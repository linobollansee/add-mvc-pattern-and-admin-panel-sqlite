# Environment Configuration File Example / Umgebungskonfigurations-Beispieldatei
# Copy this file to .env and replace with your actual values / Kopiere diese Datei zu .env und ersetze mit deinen echten Werten
# DO NOT commit .env file to version control / NICHT die .env Datei in die Versionskontrolle committen

# Session Secret - Used to sign session cookies / Session-Geheimnis - Wird zum Signieren von Session-Cookies verwendet
# Generate a strong random string (64+ characters recommended) / Generiere einen starken Zufallsstring (64+ Zeichen empfohlen)
# Example: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your-random-secret-key-here

# Admin Password - Password for accessing admin panel / Admin-Passwort - Passwort für Zugriff auf Admin-Panel
# Use a strong, unique password / Verwende ein starkes, einzigartiges Passwort
# Minimum 12 characters recommended / Mindestens 12 Zeichen empfohlen
ADMIN_PASSWORD=your-admin-password-here

# Node Environment - Application environment mode / Node-Umgebung - Anwendungsumgebungsmodus
# Options: development, production, test / Optionen: development, production, test
# Default: development / Standard: development
# NODE_ENV=development
